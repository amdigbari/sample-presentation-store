import { NextPage } from 'next';

type screenType = {};

const StoreScreen: NextPage<screenType> = () => {
    return <div className="container flex flex-col items-center">MUST complete</div>;
};

export default StoreScreen;
